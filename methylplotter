#!/usr/bin/env python
from pathlib import Path
import sys

repo_src = Path(__file__).resolve().parent / "src"
if repo_src.exists():
    sys.path.insert(0, str(repo_src))

from methylplotter.cli import main

if __name__ == "__main__":
    main()
